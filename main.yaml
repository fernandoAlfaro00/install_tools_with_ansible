- hosts: localhost
  become: yes
  become_user: root
  vars:
    current_user: "{{ lookup('env','USER') }}"
  roles:
    - role: devops
      when: install_devops | bool
    - role: virtualization
      when: install_virtualization | bool
    - role: utilities
      when: install_utilities | bool